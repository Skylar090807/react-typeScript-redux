import React, { Fragment } from 'react'

const App: React.FC = () => {
  const onClick = () => {}

  const onLogout = () => {}
  return (
    <Fragment>
      <div>로그인 중</div>
      <button>로그인</button>
      <button>로그아웃</button>
    </Fragment>
  )
}

export default App
